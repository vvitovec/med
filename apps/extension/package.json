{
  "name": "@trust-coupons/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node scripts/copy-manifest.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@trust-coupons/adapters": "file:../../packages/adapters",
    "@trust-coupons/config": "file:../../packages/config",
    "@trust-coupons/shared": "file:../../packages/shared",
    "vite": "^6.3.4",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.313",
    "jsdom": "^25.0.1",
    "vitest": "^3.1.2"
  }
}
